{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$(date -u '+%Y-%m-%d %H:%M:%S'), SessionStart\" >> /tmp/.session_status && git rev-parse HEAD 2>/dev/null > /tmp/.session_commit"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$(date -u '+%Y-%m-%d %H:%M:%S'), UserPromptSubmit\" >> /tmp/.session_status"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$(date -u '+%Y-%m-%d %H:%M:%S'), Stop\" >> /tmp/.session_status"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/send_to_slack.sh"
          }
        ]
      }
    ]
  }
}
