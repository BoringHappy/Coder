# CodeMate Environment Configuration

# Optional: Default repository URL
# GIT_REPO_URL=

# Anthropic API Configuration
# ANTHROPIC_AUTH_TOKEN=
# ANTHROPIC_BASE_URL=

# Custom Plugin Configuration
# Add your own plugin marketplaces (comma-separated GitHub repo paths)
# Example: CUSTOM_MARKETPLACES=username/my-marketplace,org/another-marketplace
# CUSTOM_MARKETPLACES=

# Add your own plugins to install (comma-separated plugin names)
# Example: CUSTOM_PLUGINS=my-plugin@my-marketplace,another-plugin@my-marketplace
# CUSTOM_PLUGINS=

# Webhook Mode Configuration (for Dockerfile.webhook)
# GitHub webhook secret for verifying webhook signatures (optional, recommended)
# GITHUB_WEBHOOK_SECRET=

# Cloudflare Tunnel token (optional — only needed if exposing via Cloudflare Tunnel)
# Without this, the server listens on port 8080 for direct access / port forwarding
# CLOUDFLARE_TUNNEL_TOKEN=

# GitHub App Authentication (alternative to GITHUB_TOKEN)
# Use these instead of GITHUB_TOKEN for server-to-server auth with scoped permissions.
# Requires a registered GitHub App with App ID and private key.
# GITHUB_APP_ID=
# GITHUB_APP_PRIVATE_KEY=           # Inline PEM content (use this OR the file option below)
# GITHUB_APP_PRIVATE_KEY_FILE=      # Path to .pem file (e.g. /home/agent/.github-app-key.pem)
# GITHUB_APP_INSTALLATION_ID=       # Optional — auto-detected from webhook payloads if omitted
